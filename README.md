# ikguard Pages

This repository hosts the public GitHub Pages management UI for ikguard.

- The site reads `licenses/license.json` and `license/public_key.json` from `linxingaifei/ikguard-core`.
- Authorization data stays in the private core repository.
- The public Pages site contains only the static UI and deployment workflow.

Open the site after Pages is enabled in repository settings.
